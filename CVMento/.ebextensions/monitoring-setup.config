files:
  "/opt/aws/bin/eip-associate.sh":
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/bin/bash
      EIP_ALLOCATION_ID="eipalloc-0a3141de40efc714b"
      INSTANCE_ID=$(curl -s http://169.254.169.254/latest/meta-data/instance-id)

      aws ec2 associate-address \
        --instance-id "$INSTANCE_ID" \
        --allocation-id "$EIP_ALLOCATION_ID" \
        --region ap-northeast-2

  "/opt/aws/bin/setup-swap.sh":
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/bin/bash
      LOG_FILE="/var/log/swap-setup.log"
      exec > >(tee -a "$LOG_FILE") 2>&1

      echo "=== Swap 메모리 설정 시작: $(date) ==="
      if [ ! -f /swapfile ]; then
        echo "1GB swap 파일 생성 중..."
        dd if=/dev/zero of=/swapfile bs=1M count=1024
        chmod 600 /swapfile
        mkswap /swapfile
      fi

      swapon /swapfile || true
      if ! grep -q "/swapfile" /etc/fstab; then
        echo '/swapfile none swap sw 0 0' >> /etc/fstab
      fi

      if ! grep -q "vm.swappiness=10" /etc/sysctl.conf; then
        echo 'vm.swappiness=10' >> /etc/sysctl.conf
      fi
      sysctl vm.swappiness=10

      echo "=== Swap 설정 완료: $(date) ==="
      free -h

  "/opt/aws/bin/install-grafana-agent.sh":
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/bin/bash
      LOG_FILE="/var/log/grafana-agent-install.log"
      exec > >(tee -a "$LOG_FILE") 2>&1

      echo "=== Grafana Agent 설치 시작: $(date) ==="

      if command -v grafana-agent &> /dev/null; then
        echo "이미 설치됨"
        grafana-agent --version || true
        exit 0
      fi

      echo "네트워크 연결 확인 중..."
      for i in {1..20}; do
        if ping -c 1 github.com &> /dev/null; then
          echo "네트워크 연결 확인됨"
          break
        fi
        echo "네트워크 대기 중... ($i/20)"
        sleep 10
      done

      # Amazon Linux 2023 은 dnf
      dnf -y update
      dnf -y install wget unzip net-tools procps-ng

      cd /tmp
      AGENT_VERSION="v0.38.1"
      wget -O grafana-agent.zip "https://github.com/grafana/agent/releases/download/${AGENT_VERSION}/grafana-agent-linux-amd64.zip"

      if [ $? -eq 0 ]; then
        unzip -o grafana-agent.zip
        chmod +x grafana-agent-linux-amd64
        mv grafana-agent-linux-amd64 /usr/local/bin/grafana-agent

        useradd --system --no-create-home --shell /bin/false grafana || true
        mkdir -p /etc/grafana-agent /var/lib/grafana-agent /var/log/grafana-agent
        chown -R grafana:grafana /var/lib/grafana-agent /var/log/grafana-agent

        echo "=== Grafana Agent 설치 완료 ==="
        grafana-agent --version || true
      else
        echo "ERROR: 다운로드 실패"
        exit 1
      fi

  "/opt/aws/bin/start-grafana-agent.sh":
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/bin/bash
      set -euo pipefail
      LOG_FILE="/var/log/grafana-agent-start.log"
      exec > >(tee -a "$LOG_FILE") 2>&1
      echo "=== Grafana Agent 시작: $(date) ==="

      # EB 환경변수 안전 로드
      ENV_FILE="/opt/elasticbeanstalk/deployment/env"
      if [ -f "$ENV_FILE" ]; then
        set -a; . "$ENV_FILE"; set +a
      else
        echo "WARN: $ENV_FILE 없음"
      fi

      # ★ container_commands 단계는 Flip 전이므로, 소스는 'staging'에서 가져온다
      SRC="/var/app/staging/agent-production.yml"
      DST="/etc/grafana-agent/agent.yml"

      if [ ! -f "$SRC" ]; then
        echo "WARN: $SRC 없음 — Grafana Agent 설정 생략"
        exit 0
      fi

      # (선택) Spring Boot 헬스 대기 — 필요 시 유지
      for i in {1..60}; do
        if curl -fsS http://localhost:5000/actuator/health >/dev/null 2>&1; then
          echo "Spring Boot 준비됨"; break
        fi
        echo "Spring Boot 대기 중... ($i/60)"; sleep 5
      done

      # 환경변수 치환(미설정이면 생략)
      install -D -m 0640 -o grafana -g grafana "$SRC" "$DST"
      : "${GRAFANA_PUSH_URL:=}"; : "${GRAFANA_USERNAME:=}"; : "${GRAFANA_PASSWORD:=}"
      if [ -n "${GRAFANA_PUSH_URL}${GRAFANA_USERNAME}${GRAFANA_PASSWORD}" ]; then
        sed -i "s|\${GRAFANA_PUSH_URL}|${GRAFANA_PUSH_URL}|g" "$DST"
        sed -i "s|\${GRAFANA_USERNAME}|${GRAFANA_USERNAME}|g" "$DST"
        sed -i "s|\${GRAFANA_PASSWORD}|${GRAFANA_PASSWORD}|g" "$DST"
      else
        echo "WARN: Grafana env 미설정 — 치환 생략"
      fi

      # systemd 유닛 작성/시작
      cat >/etc/systemd/system/grafana-agent.service <<'EOF'
      [Unit]
      Description=Grafana Agent
      After=network-online.target
      Wants=network-online.target
      [Service]
      Type=simple
      User=grafana
      Group=grafana
      ExecStart=/usr/local/bin/grafana-agent --config.file=/etc/grafana-agent/agent.yml --config.expand-env --server.http.address=0.0.0.0:12345
      Restart=always
      RestartSec=5
      WorkingDirectory=/var/lib/grafana-agent
      [Install]
      WantedBy=multi-user.target
      EOF

      systemctl daemon-reload
      systemctl stop grafana-agent || true
      systemctl enable grafana-agent
      systemctl start grafana-agent || { echo "WARN: grafana-agent start 실패(배포는 계속)"; exit 0; }

      echo "=== Grafana Agent 시작 완료: $(date) ==="

commands:
  01_associate_eip:
    command: "/opt/aws/bin/eip-associate.sh"
    ignoreErrors: true
  02_setup_swap:
    command: "/opt/aws/bin/setup-swap.sh"
    ignoreErrors: true
  03_install_grafana_agent:
    command: "/opt/aws/bin/install-grafana-agent.sh"
    ignoreErrors: true

container_commands:
  04_start_grafana_agent:
    command: "/opt/aws/bin/start-grafana-agent.sh"
    ignoreErrors: true   # 먼저 배포 성공 보장, 안정화 후 false로 조여도 됨
