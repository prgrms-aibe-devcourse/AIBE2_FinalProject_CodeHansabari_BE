name: CI/CD

on:
  push:
    branches: [ main ]

jobs:
  build:
    if: ${{ contains(github.event.head_commit.message, '[DV]') }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'corretto'
          java-version: '17'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
        working-directory: CVMento

      - name: Build with Gradle
        run: ./gradlew clean build -Dspring.profiles.active=test
        working-directory: CVMento

      - name: Check if JAR was created
        run: |
          echo "=== Current directory ==="
          pwd
          echo "=== Build directory contents ==="
          ls -la build/ || echo "No build directory"
          echo "=== Looking for JAR files ==="
          find . -name "*.jar" || echo "No JAR files found"
          echo "=== Gradle tasks that ran ==="
          ./gradlew build --dry-run | grep jar
        working-directory: CVMento

      - name: Get current time
        uses: josStorer/get-current-time@v2.0.2
        id: current-time
        with:
          format: YYYY-MM-DDTHH-mm-ss
          utcOffset: "+09:00"

      - name: Set artifact name
        run: echo "artifact=cvmento-0.0.1-SNAPSHOT.jar" >> $GITHUB_ENV
        working-directory: CVMento

      - name: Deploy to Elastic Beanstalk
        uses: einaregilsson/beanstalk-deploy@v20
        with:
          aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          application_name: ${{ secrets.APPLICATION_NAME }}
          environment_name: ${{ secrets.ENVIRONMENT_NAME }}
          version_label: github-action-${{ steps.current-time.outputs.formattedTime }}
          region: ${{ secrets.AWS_REGION }}
          deployment_package: ./CVMento/build/libs/${{ env.artifact }}